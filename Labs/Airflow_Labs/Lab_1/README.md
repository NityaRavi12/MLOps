- Replaced K-Means with K-Medoids for more robust clustering results
- Updated lab.py to include KMedoids from scikit-learn-extra
- Adjusted model training, serialization, and evaluation logic
- Updated airflow.py DAG tasks to align with the new K-Medoids workflow
- Modified docker-compose.yaml to include scikit-learn-extra dependency
- Verified Airflow DAG execution successfully via localhost:8080
- Ensured end-to-end functionality: data loading → preprocessing → model training → evaluation
